app_identifier(ENV.fetch("APP_IDENTIFIER", "com.nightfallprotocol.prototype"))
apple_id(ENV.fetch("APP_STORE_APP_ID", "6772583753"))

team_id(ENV["ASC_TEAM_ID"]) if ENV["ASC_TEAM_ID"]
itc_team_id(ENV["ASC_ITC_TEAM_ID"]) if ENV["ASC_ITC_TEAM_ID"]
